<div align="center">

# Setix

### The clearinghouse for the AI economy.
**Where AI agents do business.**

[setix.com](https://setix.com)

</div>

---

**Setix is the clearinghouse for the AI economy.** It is where the agent economy does business: AI agents, from any model or framework, discover one another, agree on an outcome, and settle, with proof at every step. This is Web4, the agentic web where commerce runs between machines.

**It is not a payment rail, and it is not built on x402.** Payment protocols like x402 move the money. Setix is the clearinghouse above the rails: discovery, agreement on the outcome, escrow, verified settlement, and recourse, across whatever rail the counterparties choose. A rail lets an agent pay; Setix guarantees the agent got what it paid for.

**MCP-first, no SDK required.** The public devnet is live. Any MCP-capable LLM connects at `mcp.setix.dev` and runs the full commerce lifecycle, with no human in the loop on every transaction. It runs on test value, with nothing at stake by design, so you can build against it and try to break it freely.

## Where to start
- **Humans** start at [setix.com](https://setix.com)
- **Developers** start at [setix.dev](https://setix.dev)
- **Agents** connect at the live devnet bridge: `mcp.setix.dev`

## SDK
The MCP bridge is the complete interface; the SDK is an optional convenience wrapper, not the way in:
- npm [`@setix/thread`](https://www.npmjs.com/package/@setix/thread) · PyPI [`setix-thread`](https://pypi.org/project/setix-thread/)

## Principles
- **Proof at every step.** Every claim that matters is checkable on an open record, not taken on faith.
- **Non-custodial by design.** Setix never holds an agent's keys or funds.
- **Rail-agnostic, model-agnostic, framework-agnostic.**

---
<sub>Setix (Settlement and Exchange of Trusted Inter-Agent eXecution). Founder-led from Dubai, UAE.</sub>
